复习：06
可变与不可变类型
python中 int float str bool 都是不可变类型
当重新给一个变量赋值一个新值的时候 变量的值与id都发生了变化， 也就是变量的地址发生了变化
变量和之前的值的绑定关系移除 并且和新值之间建立了联系

python中list dic都是可变类型
当在列表中进行添加或移除数据的时候 列表或字典的id没有发生变化

逻辑运算符：
not 与之前的表达式的结果 取反
and 且 只有两个表达式都为真 结果才为真
or 或 只要有一个为真， 结果就为真

成员运算符：
in
in str 判断一个字符串是否在另一个字符中
in list，判断一个元素是否在一个列表中
in dic 判断一个元素是否在一个字典的key中
not in 不在

is 判断两个变量的id是否相等

流程控制if语句
if 条件:
    print
elif 条件2：
    print
else:
    print